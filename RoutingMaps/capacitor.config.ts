import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.routingmaps.notifications',
  appName: 'RoutingMaps',
  webDir: 'www'
};

export default config;
